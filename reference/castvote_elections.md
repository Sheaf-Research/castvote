# Registered elections

The packaged registry of elections and their source files.

## Usage

``` r
castvote_elections()
```

## Value

A data table with one row per election: \`id\`, \`date\`,
\`jurisdiction\`, \`office\`, \`seats\`, \`vendor\`, and \`format\`.
