# Source files for an election

Source files for an election

## Usage

``` r
castvote_election_files(id)
```

## Arguments

- id:

  An election id from \[castvote_elections()\].

## Value

A data table with one row per file: \`local_name\`, \`url\`, \`sha256\`,
\`bytes\`, and \`role\`.
