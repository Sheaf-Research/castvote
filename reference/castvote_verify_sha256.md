# Verify a file checksum

Verify a file checksum

## Usage

``` r
castvote_verify_sha256(path, sha256)
```

## Arguments

- path:

  A file path.

- sha256:

  The expected SHA-256 digest.

## Value

The path, invisibly.
