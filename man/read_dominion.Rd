% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-dominion.R
\name{read_dominion}
\alias{read_dominion}
\title{Read a Dominion CVR export}
\usage{
read_dominion(zip_path, contests = NULL, files = NULL, cores = 1L)
}
\arguments{
\item{zip_path}{Path to the CVR zip, read in place.}

\item{contests}{Contest ids to keep. Defaults to the ranked contests
(`NumOfRanks > 0` or `VoteFor > 1`).}

\item{files}{Optional subset of zip entries, for testing.}

\item{cores}{Files read in parallel on unix.}
}
\value{
Canonical marks as a data table.
}
\description{
A Dominion export is a zip of `CvrExport_*.json` files plus manifest files
that name every internal id. Each session and card is one ballot sheet. Marks
are flattened and joined to the manifests, then reduced to the
[castvote_schema()] long format for recorded votes. Contest-level overvote
and undervote counts are carried through as extra columns; mapping them to
per-rank flags is not yet defined.
}
