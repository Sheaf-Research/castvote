% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{verify_tabulator}
\alias{verify_tabulator}
\title{Verify a tabulation against an official report}
\usage{
verify_tabulator(tabulation, official)
}
\arguments{
\item{tabulation}{A [tabulate_irv()] result.}

\item{official}{A list with `winner`, `rounds` (`candidate_id`, `votes`,
`round`), `eliminated` (`candidate_id`, `votes`), and `summaries`
(`round`, `total_ballots`).}
}
\value{
A list with `ok`, the per-check flags, the mismatch tables, and the
  official exhausted count.
}
\description{
Reconstructs the round states from an official round-by-round report and
compares them with a [tabulate_irv()] result: the winner, every round's
candidate totals, the elimination order, and the exhausted count.
}
